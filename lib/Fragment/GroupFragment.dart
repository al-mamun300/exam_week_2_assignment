import 'package:flutter/cupertino.dart';

class GroupFragment extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(
      alignment: Alignment.center,
      color: const Color(0xff00942e),
      child: const Text('Group Fragment'),

    );
  }
}