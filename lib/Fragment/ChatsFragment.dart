import 'package:flutter/cupertino.dart';

class ChatFragment extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(
      alignment: Alignment.center,
      color: const Color(0xfff45757),
      child: const Text('Chat Fragment'),

    );
  }
}